// 用户信息模块
// 引入用户的临时凭证
import {getUUID} from '@/utils/storage'
const state = {
  userInfo:{}, // 存储用户信息
  userTempId:getUUID()
}
const mutations = {

}
const actions = {
 
}
const getters = {
}
export default {
  state,
  mutations,
  actions,
  getters
}